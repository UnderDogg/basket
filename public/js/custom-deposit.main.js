/* ==========================================================================
 Application Initialise - Custom Deposit JS
 ========================================================================== */

$(document).ready(function(){

    console.log('Sliders initialising.');
    // Initialise the range sliders
    $('input[type="range"]').rangeslider();

    // Register click handlers.

});

function updateProductQuotation(changedSlider){
    // Extract product from changedSlider

    // FInd form fields and update the relevant fields for the view
}